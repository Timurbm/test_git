with open('test.txt', 'w') as file:
    src = file.write('git init - local rep, git add . - add files, git commit -m "" -commit, git checkout -b dev - change branch, git branch - check branches')

#print(src)
